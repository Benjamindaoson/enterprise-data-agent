# 跨境业务经营数据智能分析 Agent

这是一个企业经营与跨境业务分析 Agent 项目，包含业务流程、后端代码、Docker 配置、OpenSpec 和教学配套资料。

## 项目定位

这是从 `D:\01_项目\04_资料参考` 中整理出来的私有 GitHub 快照。保留它的目的，是防止本地唯一代码、课程改动、Prompt、Agent 工作流和项目结构丢失。

## 当前状态

- 这是资料参考区中的项目快照，不一定是生产级项目。
- README 已统一改为中文。
- 如果项目来自第三方仓库，本仓库只保存本地学习和改动快照，不覆盖原作者仓库。
- 如果项目是本地唯一目录，本仓库作为备份和后续整理入口。

## 上传内容

上传内容包括源码、文档、配置模板、测试、Prompt、OpenSpec、示例和必要的小型数据。

## 未上传内容

以下内容不会上传：

- 真实 `.env` / `.env.*`
- API Key、Token、Cookie、私钥
- `.venv`、`node_modules`
- `dist`、`build`、`.next`、`target`
- 缓存、日志、上传临时目录
- 模型文件和超过 GitHub 普通限制的大文件

## 恢复方式

克隆仓库后，根据项目中的 `requirements.txt`、`pyproject.toml`、`package.json`、`docker-compose.yml` 等文件重新安装依赖和启动。
