# Controlled Evaluation Fixtures

These deliberately small fixtures are not demo data and never substitute for the
official Iowa snapshot. They isolate failure modes that public data cannot
deterministically guarantee: duplicate current-dimension rows, missing cost data,
no dominant contributor, denied policy access, and checkpoint recovery.

The fixture names are referenced only by tagged evaluation cases. The evaluator
must report fixture provenance in any result.
